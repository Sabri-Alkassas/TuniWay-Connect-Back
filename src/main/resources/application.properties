server.port=8080
spring.config.import=optional:file:.env[.properties]
spring.application.name=tuniway-connect-back
spring.datasource.url=jdbc:postgresql://localhost:5432/tuniway_db
spring.datasource.username=tuniway_admin
spring.datasource.password=L30NSK3nNedY
# JPA
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true

# Flyway
spring.flyway.enabled=true
spring.flyway.locations=classpath:db/migration
spring.flyway.baseline-on-migrate=true
spring.flyway.validate-on-migrate=true

# SMTP (Gmail)
spring.mail.host=smtp.gmail.com
spring.mail.port=587
spring.mail.username=gg6847747@gmail.com
spring.mail.password=${SMTP_APP_PASSWORD:}
spring.mail.properties.mail.smtp.auth=true
spring.mail.properties.mail.smtp.starttls.enable=true

app.mail.from=gg6847747@gmail.com

# JWT
app.jwt.secret=${JWT_SECRET:C_H_A_Z_4_R_S_E_C_R_E_T_2_0_2_6_!}
app.jwt.access-token-expiration-minutes=${JWT_ACCESS_TOKEN_EXP_MINUTES:15}